# 底盘直线测试（2026-09-12）

工程：本目录 MecanumJog.uvprojx。只修改测试工程，共享 yyb_stm32 源码保持原样。
上电不自动运动；原有 W/S/A/D、5 号电机和舵机测试保留。

## 接线和默认假设

- STM32F407ZG，168 MHz；CAN1 PA11 RX / PA12 TX，500 kbit/s。
- 四个底盘驱动器地址必须唯一，为 1、2、3、4。CAN 两端按实物总线要求终端匹配。
- 命令 UART5：PC12 TX、PD2 RX，115200、8N1，共地，交叉连接；通过有线下载器烧录。
- 可选维特 IMU：传感器 TX 接 PD6，传感器 RX 接 PD5，共地；标准 11 字节 55 53 欧拉角帧，带校验和。默认接收 115200，要求角度输出至少 10 Hz。可以用 `imu 9600` 切换接收波特率，但不会修改传感器自身配置。
- 前进沿用旧工程方向位：地址 1/2/3/4 = 1/0/0/1。这里没有凭空指定四个地址的物理轮位；必须在实车架空验向。
- line 的距离比例暂用原车前进轴 10.3 脉冲/mm、3200 脉冲/转。换轮径、减速比、细分或安装方向后必须重新标定，毫米数不是位置测量值。

## 新命令

命令逐条发送，等待回复，使用回车或换行。不要把整段命令一次粘贴发送。

| 命令 | 行为 |
| --- | --- |
| `cancheck 1` | 查询 1 号电机原始状态回包，编号可为 1..5；不使能、不移动 |
| `wheel 1 0` | 单独点动 1 号轮，原始方向位 0；编号 1..4，方向 0/1，先 arm |
| `invert 1 1` | 反转 1 号轮在四轮联动中的方向映射；0 恢复，不影响 wheel 原始方向 |
| `trim 1 1000` | 1 号轮比例，范围 900..1100；1000 为原值。用于 line 的速度/脉冲及 straight 的速度 |
| `line W 100 30` | 前进目标 100 mm，30 RPM，同步相对位置模式，先 arm；W/S 前进/后退 |
| `straight W 2000 30` | 前进定时 2000 ms、峰值 30 RPM，保持启动时航向，先 arm；W/S 前进/后退 |
| `imu 115200` | IMU 接收串口波特率，可选 9600 或 115200 |
| `yawdir 0` | 默认纠偏极性；1 为反向 |
| `status` | 打印 IMU 新鲜度、航向（0.1 度单位）、数据年龄、方向映射及比例 |
| `stop` / `X` / `!` | 停车并取消授权；! 不需要回车 |

line 范围 20..500 mm，straight 范围 1000..5000 ms，两者速度范围 10..60 RPM。
每次使能或运动前单独发送 arm，命令执行后自动取消授权。
运动期间拒绝新的配置/运动命令，只接受 stop、X、disable 和 !。
invert、trim、yawdir、IMU 波特率都只保存在 RAM，复位后恢复默认值。

## 建议测试顺序

1. 打开本目录工程，Rebuild 后用下载器烧录。先架空四轮、让线缆远离车轮，并准备电机电源断开手段。UART5 用于运行命令，不用于本次烧录。
2. 上电应显示 ready/disarmed，不自动行驶。先逐条发送 cancheck 1、cancheck 2、cancheck 3、cancheck 4，四个地址都应有对应回包。CAN RX 仅证实收到状态报文，不代表完成运动。
3. 发送 arm，收到回复后发送 enable。再发送 arm、wheel 1 0，等待约 2 秒自动停止。逐个检查 2、3、4 号轮，必要时用方向 1 对照。标记地址对应的实际轮位。
4. 发送 arm、W。应四轮一起点动，呈前进组合。若仅某轮反向，用 invert N 1 后重试；若全部后退，应检查车头定义、整套方向映射。单轮不转或平移变成旋转时先排除地址/轮位/麦轮安装，不能靠 trim 补偿。
5. 测试期间发送单字符 !，确认停止。软件只尝试通过 CAN 停车；CAN 断线或驱动故障时要靠电机断电停止。
6. 无 IMU 也可落地做短距离测试：在平整地面标记直线，逐条发送 arm、line W 100 30。四轮应同步前进，等待 `STOP: test window elapsed` 后再发下一条。再试 arm、line S 100 30，最后逐级试 300、500 mm。line 会给出保守等待窗口再明确停车，不能把该提示当作驱动器到位确认。
7. 已接 IMU 时，静止发送 status，应为 IMU fresh。转动车身后航向应变化，恢复静止后数值应稳定。若 unavailable/stale，检查 PD6、共地、波特率和角度输出频率；没有新鲜数据时 straight 会拒绝启动。
8. 先架空发送 arm、straight W 2000 20，确认四轮协调、缓起缓停。若能安全轻微改变车身航向，观察轮速差是否趋向抵消偏转；不应把手伸向车轮。纠偏方向相反则 stop 后设置 yawdir 1，再从低速短时间重试。
9. 落地对齐标线，发送 arm、straight W 2000 30。该命令保持启动时朝向，前后各约 400 ms 速度斜坡，两秒后停车；它是定时测试，不能理解为行驶 2000 mm。再测试 S 后退和 3000 ms。明显偏转先停，不要连续加长时间。
10. 每次记录前进距离、侧偏和车头转角。小幅固定轮速差可用 trim N 995/1005 调整（每次约 0.5%，一次只改一个因素）。若车头保持平行但整车斜移，优先检查轮径、滚子方向、轮子接地和机械装配；航向反馈不能消除所有横向滑移。
11. 测试完发送 disable。记录有效的 invert/trim/yawdir 设置，复位后需重新设置。

## 控制和失败处理

straight 每 20 ms 计算角度最短路径误差，P=2 RPM/度、旋转修正限幅 ±12 RPM，四轮同步速度更新。目标角度取启动时角度，不要求 IMU 清零。
超过 250 ms 没有有效 IMU 角度帧或偏离目标超过 20 度，立即结束测试并尝试停车。
本地 jog_can.c 检查每帧发送结果，每帧最多等待约 5 ms；失败时取消该邮箱发送、放弃后续分包并请求停车。CAN 应答不等于具体电机已执行；断总线后不能保证远端电机收到停车指令。
line 没有 IMU 纠偏，也没有编码器位置反馈；straight 有航向闭环，但没有全局横向位置或距离闭环。因此最终直线精度必须实测。

## 软件验证

在本目录运行：

```powershell
& .\tests\test_contract.ps1
gcc -std=c99 -Wall -Wextra -Werror tests/test_straight.c -o Objects/test_straight.exe
& .\Objects\test_straight.exe
gcc -std=c99 -Wall -Wextra -Werror -Itests/stubs tests/test_can.c jog_can.c -o Objects/test_can.exe
& .\Objects\test_can.exe
```

主机测试验证角度跨界、纠偏极性、四轮混合、速度斜坡、IMU 校验和，以及 CAN 分包、邮箱耗尽和超时取消。还应使用 Keil 完成目标编译；主机测试不能替代实车验向和直线精度验证。
