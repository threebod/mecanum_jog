# 地图底盘路线测试（蓝牙启动）

这是两轮搬运的**底盘路线测试程序**。已安排扫码、两次原料、两次粗加工、两次暂存及回到原启停区的顺序，并加入IMU闭环旋转和连续跑圈命令。没有接入二维码识别、物料排序、机械臂流程、随机障碍检测或地面定位，因此还不是完整自主参赛程序。

## 新增：自动完整跑一遍

重新烧录本次 HEX，车体放在启停区1中心，车头朝地图上方；确认 `status` 显示 `IMU fresh`，逐条发送并等待回复：

```text
arm
route auto 1
```

启停区2使用 `route auto 2`。该命令自动使能四轮，连续完成两轮路线和旋转，经过作业点不等待、不执行扫码/抓取、不需要 `route next`，到HOME停止。除故障或急停外，不会中途等待人工继续。

“连续”表示没有工位等待和固定300 ms转折等待；平移换向、原地旋转前仍会减速到零，旋转到目标角度±2°并连续稳定200 ms后自动进入下一动作，不是全程非零速度的圆弧行驶。

单独测试旋转：在远离边界、台面和障碍的空旷处发送 `arm`，再发 `turn L 90`（左转90°）或 `turn R 90`（右转90°）。参数范围1～180°，小于等于2°在到位容差内可能不产生旋转。角度相对于收到命令时的IMU读数。先测试90°左右转均正确，再运行整条路线；`!` 随时取消。

## 固件与接线

- 工程：本目录 `MecanumJog.uvprojx`；固件：`Objects/MecanumJog.hex`。
- 蓝牙 GC：电脑此前分配的传出端口 COM6（若重新配对，以当前端口列表为准），115200、8 数据位、无校验、1 停止位。
- 命令以文本发送，勾选发送新行（CR 或 LF）。每次只发送一条，收到回复再发下一条；不要开启循环发送。
- HWT101：USART2，PD6 接 IMU TX，接收波特率默认 115200。`status` 应显示 `IMU fresh`。
- 四轮：左前 ID2/前进0，右前 ID1/前进1，左后 ID3/前进0，右后 ID4/前进1。默认反向设置为0，trim=1000，yawdir=0。
- 轮径100 mm、轴距190 mm、轮距240 mm、车体300×300 mm、G=1、1.8°、16细分（3200脉冲/电机转）。

路线采用驱动器 RPM 速度命令，平移时保持当前目标IMU航向，到指定点原地转向；平移方向根据车身当前朝向转换到四轮。旋转直接根据IMU角度闭环控制，最大10 RPM，接近目标时减速，不依靠轴距换算脉冲数。190/240 mm轮距用于验证预期角速度和转向超时范围，最终角度以IMU为准。四轮混合沿用原测试工程方向表，仍需在实车核对横移。

## 场地摆放

地图左下角为 (0,0)，右上角为 (2400,2400)，单位 mm；坐标指**车体中心**。启动时车头必须朝地图上方，即原料区所在的边，之后程序会控制转向。不要把车头朝向中心来启动。

- 启停区1：车体中心 (2250,2250)。
- 启停区2：车体中心 (2250,150)。

30 cm 车体与30 cm启停区同宽，起始摆放要准确。地图中间十字通道按400 mm、黄色方块按450 mm建模。测试场地必须先清空路径上的随机障碍。

|序号|目标中心坐标|停车行为|
|---|---|---|
|1|(2100,起点Y)|从启停区横移进入灰色通道|
|2|(2100,1200)|QR，等待扫码流程/人工确认|
|3|(2100,2080)|右上转折点，仅改变平移方向|
|4|(1200,2080)|RAW_1，等待第一轮取料|
|5|(1200,400)|COARSE_1_DROP_PICK，等待第一轮放完、再取完|
|6|(1200,1200)|中心转折点|
|7|(400,1200)|TEMP_1_DROP，等待第一轮暂存放料|
|8|(1200,1200)|中心转折点|
|9|(1200,2080)|RAW_2，等待第二轮取料|
|10|(1200,400)|COARSE_2_DROP_PICK，等待第二轮放完、再取完|
|11|(1200,1200)|中心转折点|
|12|(400,1200)|TEMP_2_STACK，等待第二轮按颜色堆叠|
|13|(1200,1200)|中心转折点|
|14|(2100,1200)|右侧通道|
|15|(2100,起点Y)|返回启停区旁|
|16|(2250,起点Y)|HOME，停止并结束|

表格中的作业等待只适用于 `route start`；`route auto` 会跳过所有作业等待，`route step` 则每个点都等待。三种模式都包含旋转：扫码点朝右、原料点朝上、粗加工点朝下、暂存点朝左；第14点恢复朝上，再返回启停区。中央400 mm窄通道和启停区内不原地旋转。

300 mm方形车体原地旋转的扫掠直径约424 mm。原料点向场内移20 mm、粗加工和暂存点向场内移50 mm，按标称地图校验了旋转圆与边界、黄色区及固定台面的净空；实际场地偏差和机械臂伸出部分仍需现场核对。这些是底盘待机位置，不是抓取位；程序不会判断扫码成功。加工台、暂存台三个槽位的逐个对准、机械臂动作及抓取距离也尚未加入。

## 第一次测试：逐点停车

1. 烧录本次生成的 HEX。先架空底盘，确认 `!` 能停止，再落地。每次复位后路线都不自动启动。
2. 发送 `status`，确认 IMU fresh 和四个前进方向。分别发送 `cancheck 1` 至 `cancheck 4`，应都有返回；这些回复只证明通讯，不证明地面移动正确。
3. 在空旷处先发送 `arm`、`enable`；再分别用 `arm` + `A`、`arm` + `D` 检查左右横移是否正确。每条命令单独发送并等动作停止。
4. 把车摆回对应启停区，车头朝上。先以启停区1为例依次发送：

   ```text
   arm
   route step 1
   ```

   `route step 1` 自动使能四个底盘电机，启动第一段150 mm左移。不要整段粘贴连发，先等 `arm` 回复。
5. 每到一点都停车并显示 `WAIT`。测量实际车体中心，和表格比较。确认位置合理后，发送 `route next`，只继续到下一个点。第2点会沿右侧通道向下走1050 mm，要先留出这段空间。
6. `route status` 可随时查询路线状态与**估计坐标**；它显示的坐标不是实测坐标。WAIT时 point 指向下一目标编号。
7. 出现方向错误、碰撞风险或明显偏离立即发送 `!`（无需新行），或发送 `stop`。急停/停止会取消整条路线；重新开始前必须把车摆回启停区，不能在中途原地重发启动命令。
8. 每一点都核对完后，回到启停区，用 `arm`、`route start 1` 测试整条路线。此模式在 QR、RAW、COARSE、TEMP 点等待 `route next`，普通转折点自动继续。HOME 自动结束。
9. 启停区2对应 `route step 2` 或 `route start 2`。起点Y改为150，其余工位顺序相同。

WAIT期间允许原有 `servo` 命令，但不会自动执行机械臂动作。应等机械动作完全结束、物料已放稳，再发 `route next`。不要在等待时手动推动或转动车身后继续；如果发生移动，应取消路线并重新定位。

## 距离标定与现阶段限制

路线以100 mm轮径换算：每转理论314.159 mm，巡航20 RPM（约105 mm/s），起止降速。距离通过累计已发速度×时间估计；没有读取编码器位移，也没有外部XY位置反馈。IMU只能控制朝向，不能纠正横向滑移、轮径误差或打滑。蓝牙断开本身不会自动停止：等待模式继续至下一个等待点，连续模式继续至终点或故障停车；测试时保持现场可断电。

在 `route_plan.h` 中分别调整：

- `ROUTE_FORWARD_SCALE`：前后实际距离 / 原先预计距离。
- `ROUTE_LATERAL_SCALE`：左右实际距离 / 原先预计距离。

默认均1.0。例如第一段预计左移150 mm而实测135 mm，将横移系数改成0.90后重新编译烧录，程序会用更长的运动时间补足预计距离。若已有系数，更新值 = 原系数 × 实测距离 / 目标距离。使用多次、双向、相同载荷与地面的测试均值；前后、左右误差不一致时，单一系数不足以解决，需要进一步定位与控制改进。

工位中心和尺寸在赛场会变化，本版取图中标称位置。调整 `routeTemplate` 时要同时保留轴向折线、车体净空并重新验证。随机障碍、地面定位、扫码和抓取完成信号接入后，才可把人工 `route next` 换成经过校验的自动事件。不能仅删除等待就称为自主参赛程序。

运动期间 IMU 超过250 ms未更新、平移航向偏差超过20°、控制间隔超过150 ms、单段平移超过45秒、一次旋转超过18秒或CAN发送失败会取消路线并尝试停车。旋转期间允许大于20°的目标角误差，目标角度±2°连续稳定200 ms后结束旋转。CAN发送成功不等于四个驱动器都执行成功；断线情况下停车命令也可能无法送达。

## 软件验证

`tests/test_route.c` 检查两种起点的折线、300 mm车体平移范围、完整旋转圆净空、回到原起点、工位数量、四轮正负号、转向后的坐标变换和速度单位。`tests/test_route_runtime.ps1` 用实际路线状态机运行理想电机/IMU模拟，检查两起点与两种IMU符号下的连续完成、±180°跨越、逐点等待、取消、独立旋转、旋转超时及IMU断流停车。它们不模拟轮胎打滑、定位误差、相机或机械臂。最终距离和工位可达性必须通过上述实车测试确认。
